# Dex runtime cache — local only, never committed
.dex/state.lock
.dex/variant-groups/
.dex/worktrees/

# Editor / OS
.DS_Store
*.swp
*~

# Environment
.env
.env.local
.env.*.local

# Node
node_modules/

# Build output
dist/
build/

# Dex runtime cache — local only, never committed
.dex/ui.json
