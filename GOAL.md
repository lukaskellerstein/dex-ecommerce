# Eshopy — Mock E2E target

This file exists to satisfy the LoopStartPanel goal-path requirement. The mock
agent backend ignores its contents and uses scripted clarification + manifest
output from `.dex/mock-config.json`.
